Setting up conf files for a new host

    cd ~
    git clone git@github.com:jch/jch-dotfiles.git .dotfiles
    cd .dotfiles
    ./setup.sh

TODO:

* layer configs for different platforms: unix-base.sh, mac.sh, linux.sh
